import React from 'react';
import LoginPanel from '../LoginPanel/LoginPanel';

function HeroNoticePanel() {
  return (
    <div className={'basis-[20%]'}>
      <LoginPanel />
    </div>
  );
}

export default HeroNoticePanel;
